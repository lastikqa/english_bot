it is just a simple project. that ive created to improve my python skills.

how to run................

put a file(.env) with your bot token = your_token in the projects root

install requirements

pip install -r requirements.txt

run main_bot.py

